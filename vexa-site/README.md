# Vexa pitch site

Public, static pitch site for Vexa, the private companion for VEX missions.

The demo uses simulated states and a presentation-only sprite atlas. It has no
API calls, analytics, wallet integration, database access, or ability to inspect
a local VEX installation.

Build with:

```bash
node build.mjs
```
